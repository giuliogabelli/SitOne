 15/11/2013 
 
- Manual for the implementation of SitOne

- Install Symfony on windows machine:
- when Symfony guide says: from you root directory (the root directoy is the directory 
where you wan to install the Symfony package) type "php composer.phar install", you 
need to type:
"/path/to/php.exe /path/to/composer.phar install"